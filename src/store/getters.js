const getters = {
  menuPermission: state => state.permissions.menuPermission,
  openedsMenu: state => state.permissions.openedsMenu,
  activeMenu: state => state.permissions.activeMenu
};
export default getters;
